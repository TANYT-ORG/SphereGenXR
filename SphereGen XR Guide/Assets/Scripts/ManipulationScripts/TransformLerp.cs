using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SphereGen.GuideXR
{

    public class TransformLerp : MonoBehaviour
    {
        public List<Vector3> TargetLocalPositions, TargetLocalScales;
        public List<Quaternion> TargetRotations;
        List<Vector3> OriginalLocalPositions, OriginalLocalScales;
        List<Quaternion> OriginalRotations;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
